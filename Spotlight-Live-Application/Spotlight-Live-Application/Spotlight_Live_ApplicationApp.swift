import SwiftUI
import Observation

@main
struct Spotlight_Live_ApplicationApp: App {
    @State private var appState = AppState()

    var body: some Scene {
        WindowGroup {
            RootView()
                .environment(appState)
        }
    }
}
